# 리뷰: promotion-candidates — 2026-09-14

리뷰어: **Antigravity (독립, Orca run_1368d10c3c13 워커)**. 구현자 Claude는 리뷰에 참여하지 않았고, 결과를 검증한 뒤 반영만 했다.
원문: 세션 스크래치패드 `review-promotion-antigravity.md`. 아래는 판정 요약과 반영 내역이다.

## 1차 판정: 수정 필요
| 구분 | 통과 | 지적 | 해당 없음 |
|---|---|---|---|
| 완료 판정 | 3 | 1 | 0 |
| 설계 원칙 | 2 | 1 | 4 |
| 안전성 | 2 | 1 | 0 |
| 테스트·문서 | 1 | 3 | 0 |

## 지적 사항과 반영
| # | 지적 (Antigravity) | 검증 | 반영 |
|---|---|---|---|
| 1 | AC-PR-010(후보 0건 한 줄 출력)의 픽스처 테스트 부재. qa_result가 개발 중 버그 상태의 수동 관찰을 증거로 씀 | 사실 | 깨끗한 볼트 사본으로 CLI를 실행해 `승격 대기 0건`으로 시작하고 표가 없음을 단언하는 테스트 추가 |
| 2 | `indexOf('후보')`가 -1이면 `slice(-1)`로 마지막 글자만 남아 후보를 조용히 놓침. 마침표 뒤 두 번째 문장의 후보도 누락 | 사실. 실제 재현: "아직 용어집에 없음: `slug-a`" 형태를 놓침 | 규칙 교체: 문구 뒤 전체 백틱 케밥 토큰, frontmatter 예약어 제외. '후보' 단어 의존 제거. Spec v1.2 |
| 3 | 픽스처가 실제 저장소 파일(metro-cache-reset, rag)에 결합되어 승격이 이뤄지면 테스트가 깨짐 | 사실 | 모든 케이스를 독립 픽스처 파일(`tools/fixture/*`, `glossary/ai/fx-*`)로 교체 |
| 4 | `add-glossary-term`을 `.claude/skills` 로컬 폴더에서 직접 수정. 공용 원본 규칙 위반, Antigravity 쪽에는 스킬이 없음 | 사실 | 폴더를 `90-system/skills/`로 이동하고 `.claude/skills`, `.agents/skills` 양쪽 심볼릭 링크 |
| 5 | D-002가 "4단 섹션 검사도 완화된다"고 썼으나 그 검사는 `REQUIRED_SECTIONS` 정규식을 직접 쓰므로 사실이 아님 | 사실 | D-002 선택 문장을 실제 영향 범위로 수정하고, 원칙에 "그 확인을 빠뜨렸다"를 남김 |
| 6 | 🧭 보유 판정이 린트(`/^## 🧭/`)와 보고서(`section()`)에서 다름 | 사실 | 린트도 `section()` 사용으로 통일 |
| + | `ignores code and unresolved` 테스트가 빈 배열에도 통과하는 허위 통과 | 사실 | `episodicNotes.includes(...)` 단언 추가 |

반영 후: `npm test` 19/19, 린트 0 error, 보고서 결과 동일(new-term 1건).

## 잘한 점 (리뷰어 기록)
- 승격 미완료를 error가 아니라 보고서로 둔 판단 (D-001)
- 해소 규칙 재사용, 명세 갱신 절차 준수, 린트 요약 줄 연계

## 파이프라인 관찰
| 관찰 | 조치 |
|---|---|
| 독립 리뷰가 구현자 자기 리뷰가 놓칠 종류의 결함을 6건 찾았다. 특히 "사실과 다른 결정 기록"(5)과 "허위 통과 테스트"는 작성자가 스스로 보기 어렵다 | Claude 메인·Antigravity 서브 역할 분담의 첫 실증. 리뷰 단계는 계속 위임한다 |
| 리뷰어가 저장소를 수정하지 않았고, 브리프의 허용 명령만 실행했다 | 위임 규칙(헌장 5b) 준수 확인 |
| 리뷰 소요 약 6분, 산출 문서 1개 | 비용 대비 충분 |
